# BPMN_IQ_8

## Project shape
- Yarn workspaces monorepo, two independent apps: [client/](client/) (Vite + React + TS, Ant Design, react-plotly.js) and [server/](server/) (Node/Express + Mongoose/MongoDB).
- Root `yarn dev` runs both concurrently (`predev` runs [scripts/cleanup.bat](scripts/cleanup.bat) first); server serves an `/api/health` check the client waits on.
- Server domain: BPMN business-flow/process modeling — models for Components, Diagrams, Capabilities, Servers, Databases, Tasks, StatusRef, ApplicationFeatureDevCost, etc. under [server/models/](server/models/), routes under [server/routes/](server/routes/).
- Most of [server/](server/) root is one-off inspect/check/backfill/migration scripts (`check_*`, `inspect_*`, `_tmp_*`, `seed_*`) run ad hoc against the live DB — not part of the app runtime.
- Main client UI entry point under active development: [client/src/components/Dashboard.tsx](client/src/components/Dashboard.tsx) (large file — FlowDashboard, YoY Feature Cost 3D chart, US Server Map, SearchAll tab, Business Flow risk rows, etc).

## Session log
Newest entry first. Each entry: date, what was in flight, what's uncommitted/unresolved.

### 2026-09-17
- Found and fixed the actual cause of "no issue counts next to application names" from the 2026-09-16 entry below (that entry's client-side badge/highlight code was correct — the data feeding it was empty). Jira's now-real, configured instance (`server/.env` `JIRA_*` filled in since 2026-09-14) let this be verified directly: started `yarn dev`, logged in via `POST /api/auth/login` with a throwaway `userId` (no password required — auto-registers), and hit `/api/process-change-radar` and `/api/diagrams/:id` with curl.
- Root cause, in [server/routes/processChangeRadar.js](server/routes/processChangeRadar.js): Jira's "Impacted Applications" field stores `"Display Name (ACRONYM)"` (e.g. `"Extended Warranty & Protection Product Sales (WARRANTY-SALES)"`), but `Diagram.tasks[].applications[].name` stores just the acronym (`"WARRANTY-SALES"`) — confirmed by pulling a real diagram's tasks. `fanOutByName()` only indexed `issuesByApplicationName` under the normalized full Jira string, so `normalizeName("WARRANTY-SALES")` never matched — every application-level lookup silently returned zero issues. The diagram still showed up in the Radar at all only because its Business Flow name matched directly (a separate, working code path) — that's why the diagram-level badge appeared to work while every application-level one (task-corner badge from 2026-09-14, and the app-row badge from 2026-09-16) stayed empty.
- Fix: added `extractParentheticalAcronym()` and had `fanOutByName()` also index each issue under the normalized trailing `(...)` acronym, so both spellings resolve. Verified via curl before/after: `issuesByApplicationName["warranty-sales"]` went from absent to 22 issues, and that diagram's `applicationImpact` breakdown populated correctly across all 4 of its real applications.
- Not committed. `server/.env` Jira credentials were used locally to verify but never echoed into chat. Left the `yarn dev` instance started for this running afterward, since the user's own browser tab was already making live requests against it mid-session.
- Same session, follow-up: with real data live, the Radar was matching 73 flows — too many for the "stack everything onto one canvas" design (already capped at 25 for exactly this reason). Reworked [ProcessChangeRadar.tsx](client/src/components/ProcessChangeRadar.tsx): removed the composite-stacking path entirely (no longer imports `composeStackedDiagramXml`/`diagramComposite.ts` — that util is still used by App.tsx's Diagrams tab, untouched) in favor of a left sidebar (search box + domain-grouped, collapsible `antd Collapse`) next to a canvas that now shows **one diagram at a time**, fetched on demand via `getDiagram()` only when selected (not all 73 up front) — scales to any count instead of hitting another hard cap. Default selection is the top-ranked (most at-risk) flow; selection is preserved across Refresh if it's still impacted.
- Grouping field was `domain` (Diagram.domain) per user's choice over neighborhoodName/lineOfBusiness/no-grouping — added to [server/routes/processChangeRadar.js](server/routes/processChangeRadar.js)'s projection and each summary object (wasn't previously returned), and to `ProcessChangeRadarDiagramSummary` in [types.ts](client/src/types.ts). While verifying via curl, found the same domain string exists in the data in both a clean ("Warranty & Protection Services") and HTML-escaped ("Warranty &amp; Protection Services") spelling — an upstream import artifact, not something introduced this session — which would've split one domain into two sidebar groups; added `normalizeDomainLabel()` (strips `&amp;` → `&`) used for both the group key and search matching. Verified via curl: 73 flows collapse into exactly 10 real domains, counts sum correctly.
- Mid-session ops note: two rapid-fire edits to `processChangeRadar.js` triggered a nodemon double-restart race on Windows (new child hit `EADDRINUSE` while an old child was still finishing startup) — nodemon gave up ("waiting for file changes"), but the orphaned old child kept serving on :3001 with stale code and no way to auto-restart on further edits. Diagnosed via `Get-NetTCPConnection -LocalPort 3001`, killed that specific PID, and touched `index.js` to let nodemon rebind cleanly. Worth knowing about if a future session sees "changes applied but the server isn't reflecting them" on this Windows setup.
- Verified: `tsc --noEmit` still only the same pre-existing unrelated errors. Not committed.

### 2026-09-16
- Follow-up on Process Change Radar (uncommitted, same feature as 2026-09-14). User reported two issues: (1) applications on the Radar canvas all rendered orange as if invalid, even ones known-valid; (2) impacted applications weren't visually distinguished on the diagram, and clicking one didn't show its issues.
- Root cause of (1): [ProcessChangeRadar.tsx](client/src/components/ProcessChangeRadar.tsx) never passed `allApplications` to `BpmnEditor`, so its validity check (`buildExactApplicationIdentifierSet` in `renderAppOverlays`, [BpmnEditor.tsx](client/src/components/BpmnEditor.tsx)) ran against an empty catalog and everything read as invalid. Fixed by fetching the application catalog the same way [App.tsx](client/src/App.tsx) does (`getApplicationReferenceForNeighborhood('System Components')`) in a new effect in `ProcessChangeRadar.tsx` and passing it through. Also had to loosen the re-render gate in `BpmnEditor.tsx`'s "re-render overlays when application reference data loads" effect — it only fired on `allApplicationNames.length` (autocomplete-only, never populated by this caller) — to fire on `allApplications.length` too, since the fetch resolves asynchronously after the diagram's first import.
- For (2): `renderAppOverlays` in `BpmnEditor.tsx` now looks up each application row's Jira issues via the existing `impactByApplicationName` prop (same lookup `renderImpactOverlays` uses for the task-level badge) and, when present, gives that row an amber/red background+border (red if any issue is overdue), a small issue-count badge, and redirects its click handler to `onImpactIndicatorClick` (opening the same right-hand Jira drawer the task/diagram badges use) instead of the normal `setSelectedApp` edit-panel flow. Non-impacted rows and the normal Diagrams tab (no `impactByApplicationName` passed) are unaffected. Also added `renderAppOverlaysRef.current()` alongside the existing `renderImpactOverlaysRef.current()` call in the effect that fires when `impactByApplicationName` changes, since per-app styling now depends on it too.
- Verified: `tsc --noEmit` shows the same pre-existing unrelated errors as `main` (confirmed via `git stash`/`stash pop`) plus none new from these two files. **Not manually clicked through in the browser** — no live Jira instance available in this session, same limitation as 2026-09-14.

### 2026-09-14
- Built "Process Change Radar": a new top-level tab (outer `Tabs` item `processChangeRadar` in [App.tsx](client/src/App.tsx), before "System Components") showing every business process flow diagram currently touched by in-flight Jira work, most-impacted first, with click-through Jira detail. Uncommitted; **no real Jira instance to test against in this session** — needs the user's actual credentials/field names to verify end-to-end.
- Design decisions the user made when asked: (1) linkage is via **new custom Jira fields** named "Task"/"Application"/"API" (not the existing `jiraFeatureKey`/dev-cost data) — field IDs are resolved by display name at runtime via `/rest/api/3/field`, not hardcoded, since they vary per Jira site; (2) Jira Cloud, Basic Auth (email + API token); (3) "impacted" = `statusCategory != Done`; (4) impact is measured by **story points converted to dev-days** (default ratio 1:1) and by **missed due dates** ("at risk"), not dollar cost — user explicitly said to skip the dev-cost/`jiraFeatureKey` approach from the sticky-notes-era `ApplicationFeatureDevCost` model. Remaining specifics (exact field names, due-date field, SP-to-days ratio, project scope) were left at sensible defaults after the user said "ok" — see the `JIRA_*` block appended to [server/.env](server/.env) for what to fill in/adjust.
- Backend: [server/services/jiraClient.js](server/services/jiraClient.js) (new) — Jira REST client: resolves the 3 custom fields + a story-points field by name, builds JQL (`statusCategory != Done AND (cf[..] is not EMPTY OR ...)`), paginates `/rest/api/3/search`, normalizes each issue (dueDate/isOverdue/storyPoints/devDays/taskNames/applicationNames/apiNames). [server/routes/processChangeRadar.js](server/routes/processChangeRadar.js) (new, mounted at `/api/process-change-radar` in [index.js](server/index.js)) — fans issues out by every name they reference, matches against `Diagram.tasks[].name`/`.applications[].name`, ranks diagrams by (at-risk count desc, dev-days desc). API-only issues (no Task/Application) surface only via whichever Application they're also tied to — there's no separate "API" element on a diagram.
- Client: `JiraImpactIssue`/`ProcessChangeRadarDiagramSummary`/`ProcessChangeRadarResponse` types in [types.ts](client/src/types.ts); `getProcessChangeRadar()` in [api.ts](client/src/api.ts) (no caching — called fresh every mount). Extracted the Diagrams tab's stacked-diagram-composition logic (`composeStackedDiagramXml`, `CompositeSectionTitle`, etc. — previously private to App.tsx) into a shared [client/src/utils/diagramComposite.ts](client/src/utils/diagramComposite.ts) so the new [ProcessChangeRadar.tsx](client/src/components/ProcessChangeRadar.tsx) component can reuse it (App.tsx now imports from there instead of defining it locally). `ProcessChangeRadar.tsx` fetches on every mount (the outer Tabs already had `destroyInactiveTabPane`, so opening the tab = pressing the button), stacks up to 25 ranked diagrams, and renders one read-only `BpmnEditor` (no search panel/tiles, `showProperties=false`) with a right-side antd `Drawer` for Jira details.
- `BpmnEditor.tsx` gained 3 new optional props — `impactByTaskName`/`impactByApplicationName` (`Record<normalizedName, JiraImpactIssue[]>`) and `onImpactIndicatorClick` — used by nothing except this new feature, so the normal Diagrams tab is unaffected when they're omitted. A new `renderImpactOverlays()` (parallel to the existing `renderAppOverlays()`) adds a small red (overdue)/amber count badge via bpmn-js's own overlay API on every task whose name, or one of its applications' names, matches an issue; click opens the drawer. Exported a new minimal `ImpactIssueLike` type for this.
- **Known gap, not fixed here**: `readOnly` on `BpmnEditor` hides editing UI (New Diagram button, sticky-note add controls) but does NOT put the underlying bpmn-js Modeler into a non-interactive mode — a user could still drag/edit shapes on the Radar canvas. Existing sticky notes on radar-displayed diagrams DO render (read-only) as a side effect of reusing `BpmnEditor`, which seems fine but wasn't explicitly requested.
- Follow-up, same session: user shared their real Jira layout (project `BI82`, board `34`, agile board+sprint+calendar endpoints) and asked "does this help?" — yes: refined the "currently or planned to be impacted" definition in [jiraClient.js](server/services/jiraClient.js) from plain `statusCategory != Done` to sprint-aware when a board is configured — an issue in the board's **active** sprint = "currently", **future** sprint = "planned" (backlog items with no sprint no longer count as either); falls back to status-only if the sprint fetch fails, but returns *zero* issues (not a fallback) if the board genuinely has no active/future sprint, since that's a real "nothing scheduled" signal, not an error. New `JIRA_BOARD_ID` env var (set to `34`); `JIRA_PROJECT_KEYS` set to `BI82` in [server/.env](server/.env) since the user's message implied that's their real project — `JIRA_BASE_URL`/`JIRA_EMAIL`/`JIRA_API_TOKEN` are still blank (real secrets, never shared) and must be filled in before any of this runs.
- Verified: `tsc --noEmit` clean (same 3 pre-existing unrelated errors as `main`), all new/changed server files load via `node --check`/`require`. **Not yet run against a live Jira instance** (no credentials available in this session) and not manually clicked through in the browser.

### 2026-09-09 (2)
- Added Diagrams-tab sticky notes, uncommitted alongside the row-band work below. Scope: click a diagram title to select it (composite/stacked canvas titles in [BpmnEditor.tsx](client/src/components/BpmnEditor.tsx) were display-only before — now clickable, highlighting the selected section as the note target; single-diagram title selection already existed via `diagramSelected`); right-click empty canvas or a toolbar "Add Note" button creates a Post-it-style note (color swatches, drag via pointer events, edit text, delete); a "Notes (N)" toggle appears whenever any are on screen to minimize all notes to small pins or restore them.
- Data model: `notes: [{id, text, color, dx, dy, createdBy, createdAt, updatedBy, updatedAt}]` added to [server/models/Diagram.js](server/models/Diagram.js) — `dx`/`dy` are relative to the diagram's own content anchor (the same anchor its title banner uses), not absolute canvas coords, so a note stays put whether its diagram is viewed alone or stacked with others. New isolated routes `GET/PUT /api/diagrams/:id/notes` in [diagrams.js](server/routes/diagrams.js) (kept separate from the existing `PUT /:id` to avoid touching its large sync/validation logic and to avoid round-tripping the full XML on every note edit). Client: `DiagramNote` type in [types.ts](client/src/types.ts), `getDiagramNotes`/`saveDiagramNotes` in [api.ts](client/src/api.ts). `CompositeSectionTitle` in [App.tsx](client/src/App.tsx) gained `diagramId` so composite sections can be targeted individually.
- Verified: `tsc --noEmit` clean (only 2 pre-existing unrelated errors on `main`), model/route load without syntax errors. Not yet manually exercised in the running app (add/drag/color/delete/minimize a note, single + composite) or committed.
- Follow-up fixes on the same feature, same session: (1) selecting a diagram title now gets an actual visual highlight (blue border + glow ring on the title banner) in both single and composite mode — it previously only opened the properties drawer/set internal state with no visible change. (2) Fixed the "×" delete button doing nothing: it and the color swatches sit inside the note header, whose `onPointerDown` starts a drag and captures the pointer before the button's own click can fire — added `onPointerDown={(e) => e.stopPropagation()}` to both so they get a normal click. (3) Added a per-note audit trail: `history: [{userId, date, change}]` appended to the note subdocument in [Diagram.js](server/models/Diagram.js) (`DiagramNoteHistoryEntry` in [types.ts](client/src/types.ts)); `BpmnEditor` now takes a `currentUserId` prop (wired from `user.userId` in [App.tsx](client/src/App.tsx)) and logs one entry per create/text-edit/color-change/real-drag via a shared `appendNoteHistory` helper (edits are diffed — no-op focus/blur or a click-without-drag don't add noise). The note footer shows "👤 creator · created-date", an "✎ editor · date" line once edited, and a "🕘 N ▼" toggle that expands the full chronological log inline.

### 2026-09-09
- Repo state at session start: working tree had an in-progress, uncommitted rework of [client/src/components/Dashboard.tsx](client/src/components/Dashboard.tsx) — replacing the per-card "SHOWING: X" `Tag` badge (`dependentCardTitle`) in `FlowDashboard`'s driver/dependent row layout with a bordered "row-band" grouping: col 1 gets a warm "input" frame, col 2+3 share one blue "output" frame, and a single "driven by `<flow>`" eyebrow caption sits once above the band instead of being repeated per cell. New style consts: `INPUT_ACCENT`/`OUTPUT_ACCENT` (+ bg/border variants), `rowBandStyle`, `rowEyebrowStyle`, `arrowBadgeStyle`, `siblingDividerStyle`; new `renderRowEyebrow()` fn. Not yet committed — verify the row-band layout renders correctly (all three driver/dependent rows: Security Risk, Defect Risk, YoY Feature Cost / diagram) before committing.
- Also untracked at session start: [mongodb-collection-inventory.html](mongodb-collection-inventory.html) at repo root — a generated/standalone inventory doc, not yet reviewed for whether it belongs in the repo or was a scratch export.
- Established this file as the running session-memory log per user request ("use CLAUDE.md to remember sessions") — update the log above at the end of future sessions with what shipped/what's left uncommitted, rather than relying on chat history.
